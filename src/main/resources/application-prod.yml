quarkus:
  datasource:
    username: ${DB_USER}
    password: ${DB_PASS}
    jdbc:
      url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}

  hibernate-orm:
    database:
      generation: update
    log:
      sql: false

kafka:
  bootstrap:
    servers: ${KAFKA_HOST}:${KAFKA_PORT}